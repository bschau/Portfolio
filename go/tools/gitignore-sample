*~
.DS_Store
._*
cmd/colconv/colconv[.exe]
cmd/hxd/hxd[.exe]
cmd/imgsize/imgsize[.exe]
cmd/kweddl/kweddl[.exe]
cmd/mren/mren[.exe]
cmd/procpwds/procpwds[.exe]
cmd/remtilde/remtilde[.exe]
cmd/sauk/sauk[.exe]
cmd/tolower/tolower[.exe]
cmd/waves/waves[.exe]
cmd/wi/wi[.exe]
cmd/zen/zen[.exe]

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Dependency directories (remove the comment below to include it)
# vendor/
